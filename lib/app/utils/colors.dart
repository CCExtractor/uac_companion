import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color.fromRGBO(22, 23, 28, 1); // default bgColor
  static const Color grayBlack = Color.fromRGBO(28, 31, 38, 1);  // alarm grid color
  // static const Color activeSwitch = Color.fromRGBO(185, 232, 192, 1); // active switch color
  static const Color green = Color(0xffAFFC41); // default bright green
  static const Color notSeleted = Color.fromRGBO(89, 95, 107, 1); // not selected color
}
